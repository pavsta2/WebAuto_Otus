services:

  selenoid3:
    image: aerokube/selenoid:1.11.3
    hostname: selenoid3
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /home/pavel/selenoid/browsers.json:/etc/selenoid/browsers.json:ro
    ports:
      - 8090:8080
    command: -container-network=selenoid3 -limit 10

  selenoid_ui:
    image: aerokube/selenoid-ui:1.10.11
    hostname: selenoid_ui
    depends_on: [selenoid3]
    command: --selenoid-uri http://selenoid3:4444

  phpadmin:
    image: 'phpmyadmin/phpmyadmin:latest'
    environment:
      - PMA_HOST=mariadb
      - PMA_PORT=3306
      - PMA_USER=bn_opencart
    ports:
      - '${PHPADMIN_PORT}:80'

  mariadb:
    image: docker.io/bitnami/mariadb:11.2
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
      - MARIADB_USER=bn_opencart
      - MARIADB_DATABASE=bitnami_opencart
    ports:
      - '3306:3306'

  opencart:
    image: agridyaev/opencart:4.0.2-3-debian-12-r33
    ports:
      - '${OPENCART_PORT}:8080'
      - '443:8443'
    environment:
      - OPENCART_HOST=${LOCAL_IP}:${OPENCART_PORT}
      - OPENCART_DATABASE_HOST=mariadb
      - OPENCART_DATABASE_PORT_NUMBER=3306
      - OPENCART_DATABASE_USER=bn_opencart
      - OPENCART_DATABASE_NAME=bitnami_opencart
      - ALLOW_EMPTY_PASSWORD=yes
    depends_on:
      - mariadb

    healthcheck:
      test: [ "CMD", "curl", "-f", "http://opencart:8080" ]
      interval: 10s
      timeout: 5s
      retries: 3

networks:
  default:
    external: true
    name: selenoid3
